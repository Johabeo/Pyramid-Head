<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.room_with_view">

    <application
        android:name="com.example.room_with_view.PyramidApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Roomwithview"
        tools:targetApi="s">
    <activity
        android:name="com.example.room_with_view.MainActivity"
        android:exported="true"
        android:theme="@style/Theme.Roomwithview">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>
    </activity>
        <activity android:name="com.example.room_with_view.NewPyramidActivity"/>



    </application>


</manifest>